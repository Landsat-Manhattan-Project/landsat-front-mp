import Button from "@mui/material/Button";

const Login = () => {
  return (
    <>
      <h1>Here will be the Login feature</h1>
      <Button variant="contained" onClick={() => alert("In progress.")}>
        Hello world
      </Button>
    </>
  );
};

export { Login };
